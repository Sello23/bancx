services:
  loan-api:
    build: .
    container_name: bancx-loan-service
    ports:
      - "8080:8080"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:h2:mem:loandb
      - SPRING_H2_CONSOLE_ENABLED=true
    restart: unless-stopped